<p id="notice"><%= notice %></p>

<h1 style="text-align: center;color: #edb009;">Pokemons</h1>
<center>
  <table style="width:80%;background-color: orange;border:1px solid white; " class="striped centered">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Tipo</th>
        <th>Categoria</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @pokemons.each do |pokemon| %>
        <tr>
          <td><%= pokemon.nome %></td>
          <td><%= pokemon.tipo %></td>
          <td><%= pokemon.categoria %></td>
          <td><%= link_to 'Show', pokemon %></td>
          <td><%= link_to 'Edit', edit_pokemon_path(pokemon) %></td>
          <td><%= link_to 'Destroy', pokemon, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</center>
<br>
  <center>
<span class="waves-effect waves-light btn" style="background-color: orange;border: 1px solid white;"><%= link_to 'New Pokemon', new_pokemon_path %></span></center>